extends ../layout

block content
  .panel.panel-default
    .panel-heading
      h1 Confirm Deletion of Task
    .panel-body
      form(action=`/saturday/delete/${date.id}`, method="post")

        button.btn.btn-primary(type="submit") Delete
        a.btn.btn-default(style="margin-left:1em", href="/saturday") Cancel
